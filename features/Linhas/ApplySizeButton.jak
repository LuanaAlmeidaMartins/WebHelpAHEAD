public refines class ApplySizeButton {
	public void size() {
		if(sizeStatus.getButtonID().equals("Linhas")) {
			WebHelpBar.applyButtonStatus.removeFontStyle("line-height");
			WebHelpBar.applyButtonStatus.setFontStyle(sizeStatus.getStyle(),
					sizeStatus.isActived());
		}
		Super().size();
	}
}