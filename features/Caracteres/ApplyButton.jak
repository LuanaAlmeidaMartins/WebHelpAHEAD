/**
 * TODO description
 */
public refines class ApplyButton {
	public void size() {
		if(sizeStatus.getButtonID().equals("Caracteres")) {
			WebHelpBar.applyButtonStatus.removeFontStyle("letter-spacing");
			WebHelpBar.applyButtonStatus.setFontStyle(sizeStatus.getStyle(),
					sizeStatus.isActived());
		}
		Super().size();
	}
}