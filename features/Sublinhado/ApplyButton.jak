/**
 * TODO description
 */
public refines class ApplyButton {
	public void simple() {
		if(simpleStatus.getButtonID().equals("Sublinhado")) {
			WebHelpBar.applyButtonStatus.setFontStyle(simpleStatus.getStyle(),
					simpleStatus.getStatus());
		}
		Super().simple();
	}
}