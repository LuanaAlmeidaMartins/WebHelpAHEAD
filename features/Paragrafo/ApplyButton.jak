
public refines class ApplyButton {

	public void size() {
		System.out.println("sizeStatus.getButtonID() "+sizeStatus.getButtonID());
		if(sizeStatus.getButtonID().equals("Paragrafos")) {
			WebHelpBar.applyButtonStatus.removeFontStyle("margin");
			WebHelpBar.applyButtonStatus.setFontStyle(sizeStatus.getStyle(),
					sizeStatus.isActived());
		}
		Super().size();
	}
}